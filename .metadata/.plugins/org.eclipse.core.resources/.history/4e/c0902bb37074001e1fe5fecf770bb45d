package chap19_enum.enums;

public enum RGB {
	// 1. 값이 여러개인 상수 선언
	RED("red", 255, 0, 0),
	GREEN("green", 0, 255, 0),
	BLUE("blue", 0, 0, 255);
	
	// 2. 위에서 지정한 값의 개수에 맞게 변수를 선언해야 한다.
	private String colorStr;
}
